cd ~/opinabox/control-api || cd ~/opinabox/control-api 2>/dev/null || echo "control-api dir not found; adjust path"

# create .env (edit values below)
cat > control-api/.env <<'ENV'
PORT=3000

# RPC -> Hardhat local node (we started it earlier on 127.0.0.1:8545)
RPC_URL=http://127.0.0.1:8545

# Contracts from your deploy output
HYPERCERT_ADDRESS=0x5FbDB2315678afecb367f032d93F642f64180aa3
ESCROW_ADDRESS=0xe7f1725E7734CE288F8367e1Bb143E90bb3F0512

# Private key of Hardhat account 0 (dev only) to call escrow.release
ESCROW_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Redis for claim tokens
REDIS_URL=redis://127.0.0.1:6379

# Optional: IA credentials (if using real Internet Archive uploads)
IA_ACCESS_KEY=
IA_SECRET_KEY=
ENV

# show file
sed -n '1,200p' control-api/.env
